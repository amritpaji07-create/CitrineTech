@model CitrineTechData.ViewModel.PartnerViewModel

@{
    ViewData["Title"] = "PartnerEdit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    
}

<div class="max-w-7xl mx-auto mt-6 px-4">
    <div class="bg-white shadow-lg rounded-lg overflow-hidden">
        <div class="bg-gray-800 text-white px-6 py-4">
            <h4 class="text-xl font-semibold">Edit Partner</h4>
        </div>
        <div class="p-6">
            <form asp-action="PartnerEdit" enctype="multipart/form-data" class="space-y-6">
                <div asp-validation-summary="ModelOnly" class="text-red-600 text-sm"></div>
                <input type="hidden" asp-for="Id" />

                <!-- Title -->
                <div>
                    <label asp-for="Name" class="block text-sm font-medium text-gray-700"></label>
                    <input asp-for="Name" class="mt-1 p-2 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500" />
                    <span asp-validation-for="Name" class="text-red-600 text-sm"></span>
                </div>



                <!-- Existing Images -->
                @if (Model.ExistingImage != null)
                {
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">Existing Images</label>
                        <div class="border rounded p-2">
                            <img src="@Url.Content("~/" + Model.ExistingImage)" class="w-full h-24 object-cover rounded" />
                        </div>
                    </div>
                }

                <!-- Upload Photo -->
                <div>
                    <label class="block text-sm font-medium text-gray-700">Upload Photo</label>
                    <input asp-for="Photo" type="file" multiple class="mt-1 p-2 block w-full border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500" />
                </div>

                <!-- Image Preview -->
                <div id="imagePreviewContainer" class="flex flex-wrap gap-4 mt-4"></div>

                <!-- Buttons -->
                <div class="flex justify-between mt-6">
                    <input type="submit" value="Update" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition" />
                    <a asp-action="Partners" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 transition">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>